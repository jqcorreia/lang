import "std:c"

struct S {
    x: i32
}

struct R {
    a: f32
}

union foo {
    f32
    u32
}

union bar {
    f64
    cstr
}

fn main() {

//     e: S = R{} 
//     b: bar = 100
//     u: foo = b
    // a := 100
    // c := a

    d: []u32 = [99999]
    printf("%d\n", d[0])
}
