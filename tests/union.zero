import "std:c"

union U {
    u64
    f64
}

fn main() {
    u: U = f64(100.0)

    printf("%f\n", u)

    match v in u {
        f64 => { printf("%f\n", v) }
    }
    // match u as v {
    //     u64 => { }
    //     f64 => { }
    // }
}
